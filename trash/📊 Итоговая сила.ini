📊 Итоговая сила
🟢 76% (средне-сильный бычий сценарий)
🔮 Прогноз
Вероятно:
В ближайшие 2–4 часа — флет или лёгкий откат к 0.4410,
После подтверждения — движение к 0.451–0.455 (цели 2–3).
Закрепление выше 0.455 откроет путь к 0.4619.
✅ Итоговая рекомендация
⚠️ Ждать подтверждения или отката к нижней границе (0.4410)
Входить после отскока с подтверждением на 15m–30m.
Хочешь, я добавлю краткий итог одной строкой в конце каждого анализа? Например:
“🔮 Сила 76% — ждём ретест 0.4410, потом рост к 0.451.”



🔹 Идея блока “📊 Итоговая сила / 🔮 Прогноз”

ML-модель и фильтры вместе могут формировать итоговую “оценку сценария” (например, 0–100 %) и краткий прогноз.
Это делает сигнал не просто техническим, а осмысленным и понятным трейдеру.

💡 Как формируется “сила сценария”

Базовая сила — берётся из final_scores, нормируется к 0–100 %.

ML-оценка — добавляется весом (например, ML-модель прогнозирует класс 3 — TP3 → значит сила высокая).

Комбинированная метрика:

strength = 0.6 * normalized_final_scores + 0.4 * ml_confidence


Далее сила переводится в словесное описание:

🔴 0–40 % — слабый / сомнительный сценарий

🟠 40–60 % — нейтральный / требующий подтверждения

🟢 60–80 % — средне-сильный бычий (или медвежий)

🟣 80–100 % — мощный импульсный сценарий

🧠 Пример вставки в сигнал

После блока фильтров и перед записью в БД/отправкой:

📊 Итоговая сила
🟢 76% (средне-сильный бычий сценарий)

🔮 Прогноз
В ближайшие 2–4 ч — флет или лёгкий откат к 0.4410,  
После подтверждения — движение к 0.451–0.455 (цели 2–3).  
Закрепление выше 0.455 откроет путь к 0.4619.

✅ Итоговая рекомендация
⚠️ Ждать подтверждения или отката к нижней границе (0.4410).  
Входить после отскока с подтверждением на 15m–30m.

🔮 Сила 76% — ждём ретест 0.4410, потом рост к 0.451.



ALTER TABLE ml_trading_data 
ADD COLUMN ml_allowed INTEGER DEFAULT NULL;
ALTER TABLE ml_trading_data 
ADD COLUMN ml_predicted_label TEXT DEFAULT '';
ALTER TABLE ml_trading_data 
ADD COLUMN ml_confidence REAL DEFAULT 0.0;
ALTER TABLE ml_trading_data 
ADD COLUMN ml_strength REAL DEFAULT 0.0;
ALTER TABLE ml_trading_data 
ADD COLUMN ml_mode TEXT DEFAULT 'advisory';
ALTER TABLE ml_trading_data 
ADD COLUMN ml_decision TEXT DEFAULT 'neutral';
ALTER TABLE ml_trading_data 
ADD COLUMN ml_comment TEXT DEFAULT '';
